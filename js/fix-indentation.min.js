const fs=require("fs"),path=require("path"),filePath=path.join(__dirname,"spa-navigation.js");let content=fs.readFileSync(filePath,"utf8");content=content.replace(/\ntry {\nconst mainContent/g,"\n        try {\n            const mainContent"),content=content.replace(/mainContent\.innerHTML = (.*?);\n    }\n\n            console\.log/g,(n,t)=>`mainContent.innerHTML = ${t};\n\n            console.log`),fs.writeFileSync(filePath,content,"utf8");